const userAccessToken = '';
const url = 'https://accounts.spotify.com/authorize';

const spotify = {
    getAccessToken() {
        if (userAccessToken) {
            return userAccessToken;
        } else {
            
        }
    }
}